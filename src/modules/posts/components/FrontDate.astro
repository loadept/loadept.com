---
import formatDate from '../../shared/utils/formatDate'

interface Props {
  date: string
}
const { date } = Astro.props
---

<div class='flex items-center mr-4'>
  <span class='text-2xl mr-2 text-[#c678dd]'></span>
  {formatDate(date, 'numeric')}
</div>
