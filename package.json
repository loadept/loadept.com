{
  "scripts": {
    "dev": "bun run --cwd ui dev",
    "build": "bun run --cwd ui build",
    "preview": "bun run --cwd ui preview",
    "astro": "bun run --cwd ui astro",
    "install:ui": "bun install --cwd ui"
  }
}
